#include <stdio.h>

/**
*main - Entry point
*Description: print small alphabets
*Return: always 0
*/

int main(void)
{
char x;
for (x = 'a'; a <= 'z' ; x++)
{
if (x == 'q' || x == 'e')
{
continue;
}
putchar(x);
putchar('\n');
return (0);
}
